spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        - id: service-producer
          uri: lb://service-producer
          predicates:
            - Path=/producer/**
        - id: service-consumer
          uri: lb://service-consumer
          predicates:
            - Path=/consumer/**
