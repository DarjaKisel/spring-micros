spring:
  cloud:
    discovery:
      enabled: true
    config:
      discovery:
        service-id: config
      fail-fast: true
      uri: http://config:8888

logging:
  level:
    service-producer: DEBUG
